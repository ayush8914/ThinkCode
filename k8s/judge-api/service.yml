apiVersion: v1
kind: Service
metadata:
  name: judge-api-service
  labels:
    app: judge-api
spec:
  type: ClusterIP
  ports:
    - port: 3008
      targetPort: 3008
      protocol: TCP
      name: http
  selector:
    app: judge-api